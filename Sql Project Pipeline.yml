# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- dev

pool:
  vmImage: ubuntu-latest

steps:
- script: echo Hello, world!
  displayName: 'Run a one-line script'
- task: SqlAzureDacpacDeployment@1
  inputs:
    azureSubscription: 'Azure subscription 1(48f468c5-e81a-41d7-b094-4aa8d9536c00)'
    AuthenticationType: 'server'
    ServerName: 'azurepipelines.database.windows.net'
    DatabaseName: 'Student'
    SqlUsername: 'Azurepipelines'
    SqlPassword: 'Rajesh123'
    deployType: 'SqlTask'
    SqlFile: 'Deployement Scripts\AlterScripts.sql'
    IpDetectionMethod: 'AutoDetect'
- script: |
    echo Add other tasks to build, test, and deploy your project.
    echo See https://aka.ms/yaml
  displayName: 'Run a multi-line script'
